/**
 * Generated by orval v8.5.2 🍺
 * Do not edit manually.
 * django-allauth: Headless API
 * # Introduction

Welcome to the django-allauth API specification. This API is intended to be
consumed by two different kind of clients:

- Web applications running in a **browser** context. For example, a
  single-page React application, to which the end user can navigate using a web
  browser.

- Applications, **apps** for short, executing in non-browser contexts. For example,
  a mobile Android or iOS application.

The security considerations for these two usage types are different. In a
browser context, cookies play a role.  Without taking special precautions, your
web application may be vulnerable to Cross-Site Request Forgery attacks.  For
mobile applications, this does not apply.

The API can be used for both use cases. Differences in handling of security is
automatically adjusted for, based on the request path used to make the API call.
For example, signing up can either be done using the
`/_allauth/browser/v1/auth/signup` or the `/_allauth/app/v1/auth/signup`
endpoint. For the **browser** usage, session cookies and CSRF protection
applies. For the **app** usage, cookies play no role, instead, a session token
is used.  The paths of all endpoints are documented in the form of
`/_allauth/{client}/v1/auth/signup`. Depending on the client type (`{client}`),
there may be slight differences in request/response handling.  This is
documented where applicable.


# Scope

The following functionality is all in scope and handled as part of this API:

- Regular accounts:
  - Login
  - Signup
  - Password forgotten
  - Manage email (add, remove, verify, select a different primary)
  - Change password.
  - Verification of email addresses.
- Two-Factor Authentication:
  - Authentication using an authenticator code
  - (De)activate TOTP
  - (Re)generate recovery codes
  - "Trust this browser"
- Third-party providers:
  - Authenticate by performing a browser-level redirect (synchronous request).
  - Authenticate by means of a provider token.
  - Connect additional provider accounts.
  - Disconnect existing provider accounts.
  - Setting a password in case no password was set, yet.
  - Querying additional information before signing up.
- Session management:
  - Listing all sessions for a user.
  - Signing out of any of those sessions.


# Browser Usage

For web applications running in a browser, routing needs to be setup correctly
such that the sessions initiated at the backend are accessible in the frontend.

## Routing

When using the API in a browser context, regular Django sessions are used, along
with the usual session cookies. There are several options for setting up the
routing of your application.


###  Single Domain Routing

With single domain, path-based routing, both your frontend and backend are
served from the same domain, for example `https://app.org`. You will have to
make sure that some paths are served by the frontend, and others by the backend.


### Sub-domain Routing

With sub-domain based routing, the frontend and backend are served from
different domains.  However, as session cookies are used, these different
domains must share common main domain.

For example, you may use `app.project.org` for the frontend, which
interfaces with the backend over at `backend.project.org`.  In this
setup, Django will need to be configured with:

```
SESSION_COOKIE_DOMAIN = "project.org"
CSRF_COOKIE_DOMAIN = "project.org"
```

If your organization hosts unrelated applications, for example, a CMS for
marketing purposes, on the top level domain (`project.org`), it is not advisable
to set the session cookie domain to `project.org`, as those other applications
could get access to the session cookie. In that case, it is advised to use
`backend.app.project.org` for the backend, and set the session cookie domain to
`app.project.org`.


# App Usage

For app based usage, cookies play no role, yet, sessions are still used. When a
user walks through the authentication flow, a session is created.  Having an
authenticated session is proof that the user is allowed to further interact with
the backend. Unauthenticated sessions are also needed to remember state while
the user proceeds to go over the required steps necessary to authenticate.


## Session Tokens

Given that there is no cookie to point to the session, the header
`X-Session-Token` is used instead. The way of working is as follows:

- If you do not have a session token yet, do not send the `X-Session-Token` header.

- When making requests, session tokens can appear in the metadata
  (`meta.session_token`) of authentication related responses. If a session
  token appears, store it (overwriting any previous session token), and ensure
  to add the token to the `X-Session-Token` header of all subsequent requests.

- When receiving an authentication related response with status code 410
  (`Gone`), that is meant to indicate that the session is no longer valid.
  Remove the session token and start clean.


## Access Tokens

While session tokens are required to handle the authentication process,
depending on your requirements, a different type of token may be needed once
authenticated.

For example, your app likely needs access to other APIs as well. These APIs may
 even be implemented using different technologies, in which case having a
 stateless token, possibly a JWT encoding the user ID, might be a good fit.

In this API and its implementation no assumptions, and no (limiting) design
decisions are made in this regard. The token strategy of django-allauth is
pluggable, such that you can expose your own access token when the user
authenticates. As for as the API specification is concerned, the access token
will appear in the response of metadata (`meta.access_token`) of a successful
authentication request. How you can customize the token strategy can be found
over at the documentation of the `allauth.headless` Django application.


# Responses

Unless documented otherwise, responses are objects with the following
properties:
- The `status`, matching the HTTP status code.
- Data, if any, is returned as part of the `data` key.
- Metadata, if any, is returned as part of the `meta` key.
- Errors, if any, are listed in the `errors` key.


# Authentication Flows

In order to become authenticated, the user must complete a flow, potentially
consisting of several steps. For example:
- A login, after which the user is authenticated.
- A Login, followed by two-factor authentication, after which the user is
  authenticated.
- A signup, followed by mandatory email verification, after which the user is
  authenticated.

The API signals to the client that (re)authentication is required by means of a
`401` or `410` status code:
- Not authenticated: status `401`.
- Re-authentication required: status `401`, with `meta.is_authenticated = true`.
- Invalid session: status `410`. This only occurs for clients of type `app`.

All authentication related responses have status `401` or `410`, and,
`meta.is_authenticated` indicating whether authentication, or re-authentication
is required.

The flows the client can perform to initiate or complete the authentication are
communicates as part of authentication related responses. The authentication can
be initiated by means of these flows:
- Login using a local account (`login`).
- Signup for a local account (`signup`).
- Login or signup using the third-party provider redirect flow (`provider_redirect`).
- Login or signup by handing over a third-party provider retrieved elsewhere (`provider_token`).
- Login using a special code (`login_by_code`).
- Login using a passkey (`mfa_login_webauthn`).
- Signup using a passkey (`mfa_signup_webauthn`).

Depending on the state of the account, and the configuration of django-allauth, the flows above
can either lead to becoming directly authenticated, or, to followup flows:
- Provider signup (`provider_signup`).
- Email verification (`verify_email`).
- Phone verification (`phone_email`).
- Two-factor authentication required (TOTP, recovery codes, or WebAuthn) (`mfa_authenticate`).
- Trust this browser (`mfa_trust`).

While authenticated, re-authentication may be required to safeguard the account when sensitive actions
are performed. The re-authentication flows are the following:
- Re-authenticate using password (`reauthenticate`).
- Re-authenticate using a 2FA authenticator (TOTP, recovery codes, or WebAuthn) (`mfa_reauthenticate`).


# Security Considerations

## Input Sanitization

The Django framework, by design, does *not* perform input sanitization. For
example, there is nothing preventing end users from signing up using `<script>`
or `Robert'); DROP TABLE students` as a first name. Django relies on its
template language for proper escaping of such values and mitigate any XSS
attacks.

As a result, any `allauth.headless` client **must** have proper XSS protection
in place as well. Be prepared that, for example, the WebAuthn endpoints could
return authenticator names as follows:

    {
      "name": "<script>alert(1)</script>",
      "credential": {
        "type": "public-key",
        ...
      }
    }
 * OpenAPI spec version: 1
 */
import {
  useMutation,
  useQuery
} from '@tanstack/react-query';
import type {
  DataTag,
  DefinedInitialDataOptions,
  DefinedUseQueryResult,
  MutationFunction,
  QueryClient,
  QueryFunction,
  QueryKey,
  UndefinedInitialDataOptions,
  UseMutationOptions,
  UseMutationResult,
  UseQueryOptions,
  UseQueryResult
} from '@tanstack/react-query';

import type {
  AddAuthenticatorConflictResponse,
  AddWebAuthnAuthenticatorBody,
  AddWebAuthnAuthenticatorResponse,
  DeleteWebAuthnBody,
  GetAllauthClientV1AccountAuthenticatorsWebauthnParams,
  ReauthenticationRequiredResponse,
  StatusOKResponse,
  UpdateWebAuthnBody,
  WebAuthnAuthenticatorResponse,
  WebAuthnCreationOptionsResponseResponse
} from '../schemas';

import { authInstance } from '../../../auth-mutator';
import type { ErrorType , BodyType } from '../../../auth-mutator';




/**
 * Returns the WebAuthn credential creation options, that can be
processed using `parseCreationOptionsFromJSON()` on the frontend.

 * @summary Get WebAuthn credential creation options

 */
export type getAllauthClientV1AccountAuthenticatorsWebauthnResponse200 = {
  data: WebAuthnCreationOptionsResponseResponse
  status: 200
}

export type getAllauthClientV1AccountAuthenticatorsWebauthnResponse401 = {
  data: ReauthenticationRequiredResponse
  status: 401
}

export type getAllauthClientV1AccountAuthenticatorsWebauthnResponse409 = {
  data: AddAuthenticatorConflictResponse
  status: 409
}

export type getAllauthClientV1AccountAuthenticatorsWebauthnResponseSuccess = (getAllauthClientV1AccountAuthenticatorsWebauthnResponse200) & {
  headers: Headers;
};
export type getAllauthClientV1AccountAuthenticatorsWebauthnResponseError = (getAllauthClientV1AccountAuthenticatorsWebauthnResponse401 | getAllauthClientV1AccountAuthenticatorsWebauthnResponse409) & {
  headers: Headers;
};

export type getAllauthClientV1AccountAuthenticatorsWebauthnResponse = (getAllauthClientV1AccountAuthenticatorsWebauthnResponseSuccess | getAllauthClientV1AccountAuthenticatorsWebauthnResponseError)

export const getGetAllauthClientV1AccountAuthenticatorsWebauthnUrl = (client: 'app' | 'browser',
    params?: GetAllauthClientV1AccountAuthenticatorsWebauthnParams,) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : value.toString())
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0 ? `/_allauth/${client}/v1/account/authenticators/webauthn?${stringifiedParams}` : `/_allauth/${client}/v1/account/authenticators/webauthn`
}

export const getAllauthClientV1AccountAuthenticatorsWebauthn = async (client: 'app' | 'browser',
    params?: GetAllauthClientV1AccountAuthenticatorsWebauthnParams, options?: RequestInit): Promise<getAllauthClientV1AccountAuthenticatorsWebauthnResponse> => {
  
  return authInstance<getAllauthClientV1AccountAuthenticatorsWebauthnResponse>(getGetAllauthClientV1AccountAuthenticatorsWebauthnUrl(client,params),
  {      
    ...options,
    method: 'GET'
    
    
  }
);}
  




export const getGetAllauthClientV1AccountAuthenticatorsWebauthnQueryKey = (client: 'app' | 'browser',
    params?: GetAllauthClientV1AccountAuthenticatorsWebauthnParams,) => {
    return [
    `/_allauth/${client}/v1/account/authenticators/webauthn`, ...(params ? [params] : [])
    ] as const;
    }

    
export const getGetAllauthClientV1AccountAuthenticatorsWebauthnQueryOptions = <TData = Awaited<ReturnType<typeof getAllauthClientV1AccountAuthenticatorsWebauthn>>, TError = ErrorType<ReauthenticationRequiredResponse | AddAuthenticatorConflictResponse>>(client: 'app' | 'browser',
    params?: GetAllauthClientV1AccountAuthenticatorsWebauthnParams, options?: { query?:Partial<UseQueryOptions<Awaited<ReturnType<typeof getAllauthClientV1AccountAuthenticatorsWebauthn>>, TError, TData>>, }
) => {

const {query: queryOptions} = options ?? {};

  const queryKey =  queryOptions?.queryKey ?? getGetAllauthClientV1AccountAuthenticatorsWebauthnQueryKey(client,params);

  

    const queryFn: QueryFunction<Awaited<ReturnType<typeof getAllauthClientV1AccountAuthenticatorsWebauthn>>> = ({ signal }) => getAllauthClientV1AccountAuthenticatorsWebauthn(client,params, { signal });

      

      

   return  { queryKey, queryFn, enabled: !!(client), ...queryOptions} as UseQueryOptions<Awaited<ReturnType<typeof getAllauthClientV1AccountAuthenticatorsWebauthn>>, TError, TData> & { queryKey: DataTag<QueryKey, TData, TError> }
}

export type GetAllauthClientV1AccountAuthenticatorsWebauthnQueryResult = NonNullable<Awaited<ReturnType<typeof getAllauthClientV1AccountAuthenticatorsWebauthn>>>
export type GetAllauthClientV1AccountAuthenticatorsWebauthnQueryError = ErrorType<ReauthenticationRequiredResponse | AddAuthenticatorConflictResponse>


export function useGetAllauthClientV1AccountAuthenticatorsWebauthn<TData = Awaited<ReturnType<typeof getAllauthClientV1AccountAuthenticatorsWebauthn>>, TError = ErrorType<ReauthenticationRequiredResponse | AddAuthenticatorConflictResponse>>(
 client: 'app' | 'browser',
    params: undefined |  GetAllauthClientV1AccountAuthenticatorsWebauthnParams, options: { query:Partial<UseQueryOptions<Awaited<ReturnType<typeof getAllauthClientV1AccountAuthenticatorsWebauthn>>, TError, TData>> & Pick<
        DefinedInitialDataOptions<
          Awaited<ReturnType<typeof getAllauthClientV1AccountAuthenticatorsWebauthn>>,
          TError,
          Awaited<ReturnType<typeof getAllauthClientV1AccountAuthenticatorsWebauthn>>
        > , 'initialData'
      >, }
 , queryClient?: QueryClient
  ):  DefinedUseQueryResult<TData, TError> & { queryKey: DataTag<QueryKey, TData, TError> }
export function useGetAllauthClientV1AccountAuthenticatorsWebauthn<TData = Awaited<ReturnType<typeof getAllauthClientV1AccountAuthenticatorsWebauthn>>, TError = ErrorType<ReauthenticationRequiredResponse | AddAuthenticatorConflictResponse>>(
 client: 'app' | 'browser',
    params?: GetAllauthClientV1AccountAuthenticatorsWebauthnParams, options?: { query?:Partial<UseQueryOptions<Awaited<ReturnType<typeof getAllauthClientV1AccountAuthenticatorsWebauthn>>, TError, TData>> & Pick<
        UndefinedInitialDataOptions<
          Awaited<ReturnType<typeof getAllauthClientV1AccountAuthenticatorsWebauthn>>,
          TError,
          Awaited<ReturnType<typeof getAllauthClientV1AccountAuthenticatorsWebauthn>>
        > , 'initialData'
      >, }
 , queryClient?: QueryClient
  ):  UseQueryResult<TData, TError> & { queryKey: DataTag<QueryKey, TData, TError> }
export function useGetAllauthClientV1AccountAuthenticatorsWebauthn<TData = Awaited<ReturnType<typeof getAllauthClientV1AccountAuthenticatorsWebauthn>>, TError = ErrorType<ReauthenticationRequiredResponse | AddAuthenticatorConflictResponse>>(
 client: 'app' | 'browser',
    params?: GetAllauthClientV1AccountAuthenticatorsWebauthnParams, options?: { query?:Partial<UseQueryOptions<Awaited<ReturnType<typeof getAllauthClientV1AccountAuthenticatorsWebauthn>>, TError, TData>>, }
 , queryClient?: QueryClient
  ):  UseQueryResult<TData, TError> & { queryKey: DataTag<QueryKey, TData, TError> }
/**
 * @summary Get WebAuthn credential creation options

 */

export function useGetAllauthClientV1AccountAuthenticatorsWebauthn<TData = Awaited<ReturnType<typeof getAllauthClientV1AccountAuthenticatorsWebauthn>>, TError = ErrorType<ReauthenticationRequiredResponse | AddAuthenticatorConflictResponse>>(
 client: 'app' | 'browser',
    params?: GetAllauthClientV1AccountAuthenticatorsWebauthnParams, options?: { query?:Partial<UseQueryOptions<Awaited<ReturnType<typeof getAllauthClientV1AccountAuthenticatorsWebauthn>>, TError, TData>>, }
 , queryClient?: QueryClient 
 ):  UseQueryResult<TData, TError> & { queryKey: DataTag<QueryKey, TData, TError> } {

  const queryOptions = getGetAllauthClientV1AccountAuthenticatorsWebauthnQueryOptions(client,params,options)

  const query = useQuery(queryOptions, queryClient) as  UseQueryResult<TData, TError> & { queryKey: DataTag<QueryKey, TData, TError> };

  return { ...query, queryKey: queryOptions.queryKey };
}




/**
 * You can alter the name of a WebAuthn credential by PUT'ting the ID and
name of the authenticator representing that credential. You can obtain
the credentials via the "List authenticators" endpoint.

 * @summary Rename a WebAuthn credential

 */
export type putAllauthClientV1AccountAuthenticatorsWebauthnResponse200 = {
  data: WebAuthnAuthenticatorResponse
  status: 200
}

export type putAllauthClientV1AccountAuthenticatorsWebauthnResponse401 = {
  data: ReauthenticationRequiredResponse
  status: 401
}

export type putAllauthClientV1AccountAuthenticatorsWebauthnResponseSuccess = (putAllauthClientV1AccountAuthenticatorsWebauthnResponse200) & {
  headers: Headers;
};
export type putAllauthClientV1AccountAuthenticatorsWebauthnResponseError = (putAllauthClientV1AccountAuthenticatorsWebauthnResponse401) & {
  headers: Headers;
};

export type putAllauthClientV1AccountAuthenticatorsWebauthnResponse = (putAllauthClientV1AccountAuthenticatorsWebauthnResponseSuccess | putAllauthClientV1AccountAuthenticatorsWebauthnResponseError)

export const getPutAllauthClientV1AccountAuthenticatorsWebauthnUrl = (client: 'app' | 'browser',) => {


  

  return `/_allauth/${client}/v1/account/authenticators/webauthn`
}

export const putAllauthClientV1AccountAuthenticatorsWebauthn = async (client: 'app' | 'browser',
    updateWebAuthnBody: UpdateWebAuthnBody, options?: RequestInit): Promise<putAllauthClientV1AccountAuthenticatorsWebauthnResponse> => {
  
  return authInstance<putAllauthClientV1AccountAuthenticatorsWebauthnResponse>(getPutAllauthClientV1AccountAuthenticatorsWebauthnUrl(client),
  {      
    ...options,
    method: 'PUT',
    headers: { 'Content-Type': 'application/json', ...options?.headers },
    body: JSON.stringify(
      updateWebAuthnBody,)
  }
);}
  



export const getPutAllauthClientV1AccountAuthenticatorsWebauthnMutationOptions = <TError = ErrorType<ReauthenticationRequiredResponse>,
    TContext = unknown>(options?: { mutation?:UseMutationOptions<Awaited<ReturnType<typeof putAllauthClientV1AccountAuthenticatorsWebauthn>>, TError,{client: 'app' | 'browser';data: BodyType<UpdateWebAuthnBody>}, TContext>, }
): UseMutationOptions<Awaited<ReturnType<typeof putAllauthClientV1AccountAuthenticatorsWebauthn>>, TError,{client: 'app' | 'browser';data: BodyType<UpdateWebAuthnBody>}, TContext> => {

const mutationKey = ['putAllauthClientV1AccountAuthenticatorsWebauthn'];
const {mutation: mutationOptions} = options ?
      options.mutation && 'mutationKey' in options.mutation && options.mutation.mutationKey ?
      options
      : {...options, mutation: {...options.mutation, mutationKey}}
      : {mutation: { mutationKey, }};

      


      const mutationFn: MutationFunction<Awaited<ReturnType<typeof putAllauthClientV1AccountAuthenticatorsWebauthn>>, {client: 'app' | 'browser';data: BodyType<UpdateWebAuthnBody>}> = (props) => {
          const {client,data} = props ?? {};

          return  putAllauthClientV1AccountAuthenticatorsWebauthn(client,data,)
        }



        


  return  { mutationFn, ...mutationOptions }}

    export type PutAllauthClientV1AccountAuthenticatorsWebauthnMutationResult = NonNullable<Awaited<ReturnType<typeof putAllauthClientV1AccountAuthenticatorsWebauthn>>>
    export type PutAllauthClientV1AccountAuthenticatorsWebauthnMutationBody = BodyType<UpdateWebAuthnBody>
    export type PutAllauthClientV1AccountAuthenticatorsWebauthnMutationError = ErrorType<ReauthenticationRequiredResponse>

    /**
 * @summary Rename a WebAuthn credential

 */
export const usePutAllauthClientV1AccountAuthenticatorsWebauthn = <TError = ErrorType<ReauthenticationRequiredResponse>,
    TContext = unknown>(options?: { mutation?:UseMutationOptions<Awaited<ReturnType<typeof putAllauthClientV1AccountAuthenticatorsWebauthn>>, TError,{client: 'app' | 'browser';data: BodyType<UpdateWebAuthnBody>}, TContext>, }
 , queryClient?: QueryClient): UseMutationResult<
        Awaited<ReturnType<typeof putAllauthClientV1AccountAuthenticatorsWebauthn>>,
        TError,
        {client: 'app' | 'browser';data: BodyType<UpdateWebAuthnBody>},
        TContext
      > => {
      return useMutation(getPutAllauthClientV1AccountAuthenticatorsWebauthnMutationOptions(options), queryClient);
    }
    /**
 * @summary Delete a WebAuthn credential

 */
export type deleteAllauthClientV1AccountAuthenticatorsWebauthnResponse200 = {
  data: StatusOKResponse
  status: 200
}

export type deleteAllauthClientV1AccountAuthenticatorsWebauthnResponse401 = {
  data: ReauthenticationRequiredResponse
  status: 401
}

export type deleteAllauthClientV1AccountAuthenticatorsWebauthnResponseSuccess = (deleteAllauthClientV1AccountAuthenticatorsWebauthnResponse200) & {
  headers: Headers;
};
export type deleteAllauthClientV1AccountAuthenticatorsWebauthnResponseError = (deleteAllauthClientV1AccountAuthenticatorsWebauthnResponse401) & {
  headers: Headers;
};

export type deleteAllauthClientV1AccountAuthenticatorsWebauthnResponse = (deleteAllauthClientV1AccountAuthenticatorsWebauthnResponseSuccess | deleteAllauthClientV1AccountAuthenticatorsWebauthnResponseError)

export const getDeleteAllauthClientV1AccountAuthenticatorsWebauthnUrl = (client: 'app' | 'browser',) => {


  

  return `/_allauth/${client}/v1/account/authenticators/webauthn`
}

export const deleteAllauthClientV1AccountAuthenticatorsWebauthn = async (client: 'app' | 'browser',
    deleteWebAuthnBody: DeleteWebAuthnBody, options?: RequestInit): Promise<deleteAllauthClientV1AccountAuthenticatorsWebauthnResponse> => {
  
  return authInstance<deleteAllauthClientV1AccountAuthenticatorsWebauthnResponse>(getDeleteAllauthClientV1AccountAuthenticatorsWebauthnUrl(client),
  {      
    ...options,
    method: 'DELETE',
    headers: { 'Content-Type': 'application/json', ...options?.headers },
    body: JSON.stringify(
      deleteWebAuthnBody,)
  }
);}
  



export const getDeleteAllauthClientV1AccountAuthenticatorsWebauthnMutationOptions = <TError = ErrorType<ReauthenticationRequiredResponse>,
    TContext = unknown>(options?: { mutation?:UseMutationOptions<Awaited<ReturnType<typeof deleteAllauthClientV1AccountAuthenticatorsWebauthn>>, TError,{client: 'app' | 'browser';data: BodyType<DeleteWebAuthnBody>}, TContext>, }
): UseMutationOptions<Awaited<ReturnType<typeof deleteAllauthClientV1AccountAuthenticatorsWebauthn>>, TError,{client: 'app' | 'browser';data: BodyType<DeleteWebAuthnBody>}, TContext> => {

const mutationKey = ['deleteAllauthClientV1AccountAuthenticatorsWebauthn'];
const {mutation: mutationOptions} = options ?
      options.mutation && 'mutationKey' in options.mutation && options.mutation.mutationKey ?
      options
      : {...options, mutation: {...options.mutation, mutationKey}}
      : {mutation: { mutationKey, }};

      


      const mutationFn: MutationFunction<Awaited<ReturnType<typeof deleteAllauthClientV1AccountAuthenticatorsWebauthn>>, {client: 'app' | 'browser';data: BodyType<DeleteWebAuthnBody>}> = (props) => {
          const {client,data} = props ?? {};

          return  deleteAllauthClientV1AccountAuthenticatorsWebauthn(client,data,)
        }



        


  return  { mutationFn, ...mutationOptions }}

    export type DeleteAllauthClientV1AccountAuthenticatorsWebauthnMutationResult = NonNullable<Awaited<ReturnType<typeof deleteAllauthClientV1AccountAuthenticatorsWebauthn>>>
    export type DeleteAllauthClientV1AccountAuthenticatorsWebauthnMutationBody = BodyType<DeleteWebAuthnBody>
    export type DeleteAllauthClientV1AccountAuthenticatorsWebauthnMutationError = ErrorType<ReauthenticationRequiredResponse>

    /**
 * @summary Delete a WebAuthn credential

 */
export const useDeleteAllauthClientV1AccountAuthenticatorsWebauthn = <TError = ErrorType<ReauthenticationRequiredResponse>,
    TContext = unknown>(options?: { mutation?:UseMutationOptions<Awaited<ReturnType<typeof deleteAllauthClientV1AccountAuthenticatorsWebauthn>>, TError,{client: 'app' | 'browser';data: BodyType<DeleteWebAuthnBody>}, TContext>, }
 , queryClient?: QueryClient): UseMutationResult<
        Awaited<ReturnType<typeof deleteAllauthClientV1AccountAuthenticatorsWebauthn>>,
        TError,
        {client: 'app' | 'browser';data: BodyType<DeleteWebAuthnBody>},
        TContext
      > => {
      return useMutation(getDeleteAllauthClientV1AccountAuthenticatorsWebauthnMutationOptions(options), queryClient);
    }
    /**
 * @summary Add a WebAuthn credential

 */
export type postAllauthClientV1AccountAuthenticatorsWebauthnResponse200 = {
  data: AddWebAuthnAuthenticatorResponse
  status: 200
}

export type postAllauthClientV1AccountAuthenticatorsWebauthnResponse401 = {
  data: ReauthenticationRequiredResponse
  status: 401
}

export type postAllauthClientV1AccountAuthenticatorsWebauthnResponse409 = {
  data: AddAuthenticatorConflictResponse
  status: 409
}

export type postAllauthClientV1AccountAuthenticatorsWebauthnResponseSuccess = (postAllauthClientV1AccountAuthenticatorsWebauthnResponse200) & {
  headers: Headers;
};
export type postAllauthClientV1AccountAuthenticatorsWebauthnResponseError = (postAllauthClientV1AccountAuthenticatorsWebauthnResponse401 | postAllauthClientV1AccountAuthenticatorsWebauthnResponse409) & {
  headers: Headers;
};

export type postAllauthClientV1AccountAuthenticatorsWebauthnResponse = (postAllauthClientV1AccountAuthenticatorsWebauthnResponseSuccess | postAllauthClientV1AccountAuthenticatorsWebauthnResponseError)

export const getPostAllauthClientV1AccountAuthenticatorsWebauthnUrl = (client: 'app' | 'browser',) => {


  

  return `/_allauth/${client}/v1/account/authenticators/webauthn`
}

export const postAllauthClientV1AccountAuthenticatorsWebauthn = async (client: 'app' | 'browser',
    addWebAuthnAuthenticatorBody: AddWebAuthnAuthenticatorBody, options?: RequestInit): Promise<postAllauthClientV1AccountAuthenticatorsWebauthnResponse> => {
  
  return authInstance<postAllauthClientV1AccountAuthenticatorsWebauthnResponse>(getPostAllauthClientV1AccountAuthenticatorsWebauthnUrl(client),
  {      
    ...options,
    method: 'POST',
    headers: { 'Content-Type': 'application/json', ...options?.headers },
    body: JSON.stringify(
      addWebAuthnAuthenticatorBody,)
  }
);}
  



export const getPostAllauthClientV1AccountAuthenticatorsWebauthnMutationOptions = <TError = ErrorType<ReauthenticationRequiredResponse | AddAuthenticatorConflictResponse>,
    TContext = unknown>(options?: { mutation?:UseMutationOptions<Awaited<ReturnType<typeof postAllauthClientV1AccountAuthenticatorsWebauthn>>, TError,{client: 'app' | 'browser';data: BodyType<AddWebAuthnAuthenticatorBody>}, TContext>, }
): UseMutationOptions<Awaited<ReturnType<typeof postAllauthClientV1AccountAuthenticatorsWebauthn>>, TError,{client: 'app' | 'browser';data: BodyType<AddWebAuthnAuthenticatorBody>}, TContext> => {

const mutationKey = ['postAllauthClientV1AccountAuthenticatorsWebauthn'];
const {mutation: mutationOptions} = options ?
      options.mutation && 'mutationKey' in options.mutation && options.mutation.mutationKey ?
      options
      : {...options, mutation: {...options.mutation, mutationKey}}
      : {mutation: { mutationKey, }};

      


      const mutationFn: MutationFunction<Awaited<ReturnType<typeof postAllauthClientV1AccountAuthenticatorsWebauthn>>, {client: 'app' | 'browser';data: BodyType<AddWebAuthnAuthenticatorBody>}> = (props) => {
          const {client,data} = props ?? {};

          return  postAllauthClientV1AccountAuthenticatorsWebauthn(client,data,)
        }



        


  return  { mutationFn, ...mutationOptions }}

    export type PostAllauthClientV1AccountAuthenticatorsWebauthnMutationResult = NonNullable<Awaited<ReturnType<typeof postAllauthClientV1AccountAuthenticatorsWebauthn>>>
    export type PostAllauthClientV1AccountAuthenticatorsWebauthnMutationBody = BodyType<AddWebAuthnAuthenticatorBody>
    export type PostAllauthClientV1AccountAuthenticatorsWebauthnMutationError = ErrorType<ReauthenticationRequiredResponse | AddAuthenticatorConflictResponse>

    /**
 * @summary Add a WebAuthn credential

 */
export const usePostAllauthClientV1AccountAuthenticatorsWebauthn = <TError = ErrorType<ReauthenticationRequiredResponse | AddAuthenticatorConflictResponse>,
    TContext = unknown>(options?: { mutation?:UseMutationOptions<Awaited<ReturnType<typeof postAllauthClientV1AccountAuthenticatorsWebauthn>>, TError,{client: 'app' | 'browser';data: BodyType<AddWebAuthnAuthenticatorBody>}, TContext>, }
 , queryClient?: QueryClient): UseMutationResult<
        Awaited<ReturnType<typeof postAllauthClientV1AccountAuthenticatorsWebauthn>>,
        TError,
        {client: 'app' | 'browser';data: BodyType<AddWebAuthnAuthenticatorBody>},
        TContext
      > => {
      return useMutation(getPostAllauthClientV1AccountAuthenticatorsWebauthnMutationOptions(options), queryClient);
    }
    