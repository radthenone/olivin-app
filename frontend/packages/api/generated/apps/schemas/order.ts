/**
 * Generated by orval v8.10.0 🍺
 * Do not edit manually.
 * olivin-app
 * API documentation
 * OpenAPI spec version: 1.0.0
 */
import type { Money } from './money';
import type { OrderItem } from './orderItem';
import type { OrderStatusEnum } from './orderStatusEnum';

/**
 * Zamówienie w postaci, w jakiej ogląda je klient.

Kwoty wychodzą policzone: klient nie składa sumy z pozycji, bo to
backend rozstrzyga, ile jest do zapłaty (ADR 0012).
 */
export interface Order {
  readonly id: string;
  /** Numer, którym klient posługuje się w kontakcie ze sklepem */
  readonly number: string;
  /** Etap cyklu życia zamówienia

  * `pending` - Oczekuje na zapłatę
  * `paid` - Opłacone
  * `in_production` - W produkcji
  * `packed` - Spakowane
  * `shipped` - Wysłane
  * `delivered` - Dostarczone
  * `cancelled` - Anulowane
  * `returned` - Zwrócone */
  readonly status: OrderStatusEnum;
  /** Adres, pod który idzie potwierdzenie. Zawsze wypełniony — także dla klienta z kontem, bo zamówienie ma zostać czytelne po anonimizacji konta. */
  readonly email: string;
  /** Imię i nazwisko odbiorcy z chwili złożenia zamówienia */
  readonly recipientName: string;
  readonly street: string;
  readonly street2: string;
  readonly city: string;
  readonly postalCode: string;
  /** Kod kraju ISO 3166-1 alfa-2 z chwili złożenia zamówienia */
  readonly country: string;
  /** Nazwa metody z chwili złożenia — cennik wolno potem zmienić */
  readonly shippingMethodName: string;
  /** Kod waluty ISO 4217 */
  readonly currency: string;
  /**
     * Kurs, po którym przeliczono ceny źródłowe na walutę zamówienia. Jeden przy sprzedaży w złotych (ADR 0019).
     * @pattern ^-?\d{0,6}(?:\.\d{0,6})?$
     */
  readonly exchangeRate: string;
  /** Kopia oznaczenia wersji regulaminu z chwili złożenia */
  readonly termsVersion: string;
  readonly items: readonly OrderItem[];
  readonly goodsTotal: Money;
  readonly shippingCost: Money;
  readonly discountAmount: Money;
  readonly couponAmount: Money;
  readonly total: Money;
  /** Timestamp when the record was created */
  readonly createdAt: string;
}
