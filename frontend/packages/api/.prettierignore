# Klient API generuje Orval (`task ovral:generate`). Formatuje go generator, nie
# my — sprawdzanie go Prettierem tylko psuje CI po każdej regeneracji, a poprawki
# i tak zniknęłyby przy następnym przebiegu. Ten sam katalog jest wykluczony w
# `.pre-commit-config.yaml` i we wspólnych regułach ESLinta; trzymaj wszystkie
# trzy wpisy zgodne.
generated/

node_modules/
